#include <bits/stdc++.h>

using namespace std;

/*int main()
{
    int l,r,a; cin >> l >> r >> a;
    int total;
    if(l > r){
        int temp = l;
        l = r;
        r = temp;
    }
    total = l + a;
    if(total <= r){
        total *= 2;
    }
    else{
        total -= r;
        total /= 2;
        total += r;
        total *= 2;
    }
    cout << total << endl;


    return 0;
}*/
